spring:
  application:
    name: data-catalogue
  cloud:
    consul:
      host: localhost
      config:
        fail-fast: false
        format: yaml
      discovery:
        fail-fast: false
        prefer-ip-address: true

---
spring:
  profiles: prod
  cloud:
    consul:
      host: consul-consul-server